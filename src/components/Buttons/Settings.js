import React from "react";
import "./buttons.css";

const Settings = () => {
	return (
		<>
			<button className="settings-btn">
				Go to Settings
				<i className="fas fa-cog timer-icon"></i>
			</button>
		</>
	);
};

export default Settings;
